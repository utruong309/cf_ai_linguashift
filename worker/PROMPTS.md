“Outline a reasonable architecture using Workers, Durable Objects, Workers AI, and a lightweight frontend. Keep it simple and practical.”

“I want to implement two endpoints: /api/detect and /api/rewrite. Show a good pattern for calling Workers AI (env.AI.run) with structured prompts and consistent JSON handling.”

“Help me write a stable prompt for jargon detection that returns only JSON with fields: terms[] and overall_score. Keep the format strict so parsing is reliable.”

“Suggest a rewrite prompt that adapts tone and audience, removes jargon, and outputs only the rewritten message without explanations.”

"Scaffold code for text input, jargon detection, audience options FE"